# Audit History & Memory

This file serves as the memory for the Dual-LLM Phase Cycle System, preventing logic regressions by tracking RCA, fixes, and overrides across builds.

---

> **PHASE:** Build (Core Hardening) | **STATUS:** Resolved
> * **Issue:** Hardcoded API key warnings, fragile JSON parsing, duplicate database record crashes.
> * **Root Cause:** Initial discovery build was a "happy-path" prototype missing error handling, fail-fast validations, and strict schema enforcement.
> * **Resolution:** Migrated to Pydantic outputs, wrapped scraper tools with safe exception handlers, added `save_leads_gracefully` DB handling, and created a manual `dispatcher.py` for Human-in-the-Loop review.
> * **Human Note:** Do not revert to raw JSON parsing (`json.loads()`). Always use `output_pydantic` to ensure LLM outputs match the exact structured schema.

> **PHASE:** Test (CI/CD Setup) | **STATUS:** Resolved
> * **Issue:** Tests could not be executed locally due to missing environment dependencies (`pytest`).
> * **Root Cause:** Virtual environment was disconnected/missing from the local shell context.
> * **Resolution:** Offloaded test execution to a GitHub Actions CI pipeline (`.github/workflows/pytest.yml`) to guarantee a fresh, repeatable test environment.
> * **Human Note:** Continue to use the CI pipeline as the source of truth for test validity rather than struggling with local pathing issues.
